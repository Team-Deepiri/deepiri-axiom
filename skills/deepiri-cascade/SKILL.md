---
name: deepiri-cascade
description: Cross-repository dependency version alignment.
---

# Deepiri Cascade

Repo: `deepiri-cascade`.

When bumping shared libs (`deepiri-modelkit`, `deepiri-shared-utils`), run cascade alignment across dependents.
