# CLAUDE.local.md — machine-local only

**Optional.** Add this file to `.gitignore` if it contains secrets or machine-specific paths.

Shared **Deepiri Genius / AXIOM** instructions live in **`CLAUDE.md`** (regenerate with `python3 path/to/deepiri-axiom/setup.py install --target .`).

Use **this** file for:

- Local-only env hints, paths, or tooling aliases
- Personal overrides that must not be committed
