---
description: Run an AXIOM-style architecture scan or diagnosis on the current codebase
---

Apply the **SCAN** protocol from project **CLAUDE.md** / **Deepiri Genius** context: cartography → seams → smells → architectural audit, then output the **CODEBASE DIAGNOSIS** structure (overview, pattern, stack, findings, strengths, actions, risk map). Cite `docs/` paths when making platform claims.
